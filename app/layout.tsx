import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'PricePK - Best Tech Prices in Pakistan',
  description: 'Compare prices for mobile phones, tablets, laptops and accessories in Pakistan. Find the best deals on Samsung, Apple, Xiaomi, and more.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
